{{- if .install_packages -}}

{{- if eq .chezmoi.os "windows" }}

{{ template "pwsh_refresh_path" . }}

{{ template "pwsh_scoop_install" . }}

# Installed this early because scoop needs git for bucket operations (011 adds
# the extras bucket) and because the neovim plugin managers shell out to it.
# scoop's git is per-user, so this no longer needs elevation.
echo "Installing git via scoop"
Invoke-ScoopInstall git
echo "Finished installing git"

# end of windows
{{- end -}}

# end of install_packages
{{- end -}}
