# My mise setup
eval "$($HOME/.local/bin/mise activate zsh)"
# Temporarily disable adding mise shims to PATH. It should in theory be enough to activate it.
# export PATH="$HOME/.local/share/mise/shims:$PATH"

